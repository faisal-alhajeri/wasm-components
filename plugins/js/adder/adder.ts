export const add = {
  add(x: number, y: number): number {
    return x + y;
  },
  sub(x: number, y: number): number {
    return x - y;
  },
  mul(x: number, y: number): number {
    return x * y;
  },
};
