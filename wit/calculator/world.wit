package docs:calculator@0.1.0;

interface calculate {
    enum op {
        add,
    }
    // should return the string (the operation of <x> <op> <y> = <result>)
    eval-expression: func(op: op, x: u32, y: u32) -> string;
}

world calculator {
    export calculate;
    import docs:adder/add@0.1.0;
}

world app {
    import calculate;
}